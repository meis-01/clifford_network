activations:
  - complex_tanh

init_methods:
  - structured_preserve
  - trabelsi

in_features: 3072
hidden_size: 128
n_layers: 5000          # ← reduce from 5000; start small
epochs: 50
batch_size: 128
device: cpu          # ← consider cuda if available
monitor_layers: [4, 8, 20, 30, 40, 49]   # ← match n_layers
dataset_name: cifar10
dataset_root: data
output_dir: cifar10_fft_results
out_features: 10      # ← add this explicitly