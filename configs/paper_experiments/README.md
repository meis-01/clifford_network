# Paper Experiment Configs

This directory is intentionally reserved for frozen configurations that produce
paper figures and tables. During development, use `configs/smoke/` and
`configs/experiments/`. Once an experiment design is final, copy the exact
config here and do not change it except by creating a new named config.
